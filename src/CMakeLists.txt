file(GLOB LOCAL_HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)
set(LOCAL_SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/file-max.c
)

set(APP_SOURCE_FILES
    ${APP_SOURCE_FILES}
    ${LOCAL_SOURCES}
    ${LOCAL_HEADERS}
    PARENT_SCOPE
)

set(APP_INCLUDE_DIRS
    ${APP_INCLUDE_DIRS}
    ${CMAKE_CURRENT_SOURCE_DIR}
    PARENT_SCOPE
)
